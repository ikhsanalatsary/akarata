export namespace stemmerUtility {
  export function isStartsWith(
    text: string,
    byTextLength: number,
    prefix: string
  ): boolean;

  export function isEndsWith(
    text: string,
    byTextLength: number,
    suffix: string
  ): boolean;
}
